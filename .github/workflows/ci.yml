name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build -o flang .

      - name: Check example files
        run: |
          for fg in exemplos/*/inicio.fg; do
            echo "Checking $fg ..."
            ./flang check "$fg"
          done
